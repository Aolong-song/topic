<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.topic.mapper.TopicMapper">
    <select id="list" resultType="com.example.topic.domain.Topic">
        select  id,
                gmt_create,
                gmt_modified,
                is_deleted,
                pic_url_list,
                content
                from topic where is_deleted=0
    </select>
    <select id="detail" parameterType="integer" resultType="com.example.topic.domain.Topic">
        select  id,
                gmt_create,
                gmt_modified,
                is_deleted,
                pic_url_list,
                content
                from topic where id=#{id} and is_deleted=0
    </select>
    <insert id="create" parameterType="com.example.topic.domain.Topic" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into topic(
            gmt_create,
            gmt_modified,
            is_deleted,
            pic_url_list,
            content
        )values(
            CURRENT_TIMESTAMP,
            CURRENT_TIMESTAMP,
            0,
            #{picUrlList},
            #{content}
        )
    </insert>
    <update id="update" parameterType="com.example.topic.domain.Topic">
        update topic
        set gmt_modified=CURRENT_TIMESTAMP,
            is_deleted=#{beDeleted},
            pic_url_list=#{picUrlList},
            content=#{content}
        where id=#{id}
    </update>
    <update id="delete">
        update topic
        set is_deleted = 1
        where id=#{id} and is_deleted=0
    </update>
</mapper>